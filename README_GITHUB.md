# 🧠 Lab6 - Нейросетевое Web-приложение

Полнофункциональное веб-приложение для работы с нейросетями: определение поз, классификация изображений и генерация изображений через Pix2Pix.

## ✨ Возможности

- **Pose Detection** — определение 17 ключевых точек тела на фотографии с помощью MoveNet
- **Image Classification** — классификация объектов в изображении с помощью MobileNet v2
- **Pix2Pix Generation** — генерация изображений обоев из эскизов через U-Net

## 🏗️ Архитектура

```
┌─────────────────┐
│   React + Vite  │ (Port 5173)
│   Фронтенд UI   │
└────────┬────────┘
         │ (прямые запросы)
┌────────▼────────┐
│ Python + Flask  │ (Port 3001)
│ U-Net Generator │
└─────────────────┘
```

## 🚀 Быстрый старт

### Требования
- Node.js 16+ 
- Python 3.8+
- 2 GB RAM (рекомендуется)

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/YOUR_USERNAME/lab6_proj.git
cd lab6_proj
```

2. **Установите npm пакеты (фронтенд)**
```bash
npm install
```

3. **Установите Python зависимости (бэкенд)**
```bash
cd server
python -m venv venv

venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # macOS/Linux

pip install -r requirements.txt
cd ..
```

### Запуск (два терминала)

**Терминал 1 - Python Flask сервер:**
```bash
cd server
venv\Scripts\activate
python pix2pix_server.py
```

**Терминал 2 - React dev сервер:**
```bash
npm run dev
```

Откройте браузер: **http://localhost:5173**

📖 **Полная инструкция**: см. [SETUP.md](./SETUP.md)

## 📁 Структура проекта

```
lab6_proj/
├── src/                      # Исходный код фронтенда (React)
│   ├── pages/               # Компоненты страниц
│   │   ├── HomePage.jsx
│   │   ├── PosePage.jsx
│   │   ├── ClassifyPage.jsx
│   │   └── Pix2PixPage.jsx
│   ├── components/          # Переиспользуемые компоненты
│   │   └── ImageUpLoader.jsx
│   ├── utils/               # Утилиты и хелперы
│   │   ├── tfloader.js      # Управление TensorFlow.js моделями
│   │   ├── pix2pix.js
│   │   └── imageHelpers.js
│   ├── App.jsx              # Корневой компонент
│   ├── main.jsx             # Точка входа
│   └── App.css              # Глобальные стили
├── server/                  # Бэкенд на Python
│   ├── pix2pix_server.py   # Flask сервер с U-Net
│   ├── requirements.txt     # Python зависимости
\
_Примечание:_ ранее в `server/` был Node.js прокси (`index.js`, `package.json`), но он удалён — фронтенд теперь делает прямые запросы к Flask.
├── package.json            # Основные npm зависимости
├── vite.config.js          # Конфигурация Vite
└── README.md               # Этот файл
```

## 🧠 Используемые модели

| Страница | Модель | Тип | Место запуска |
|----------|--------|-----|---------------|
| Pose | MoveNet | Pose Detection | Браузер (TensorFlow.js) |
| Classify | MobileNet v2 | Image Classification | Браузер (TensorFlow.js) |
| Pix2Pix | U-Net (собственная) | Image Generation | Python сервер (PyTorch) |

## 📊 Тех. стек

### Фронтенд
- **React 19** — UI фреймворк
- **Vite** — сборка и dev сервер
- **React Router DOM** — маршрутизация
- **TensorFlow.js** — ML в браузере
- **react-dropzone** — загрузка файлов

### Бэкенд
- **Python 3.8+** — язык программирования
- **Flask** — веб-фреймворк
- **PyTorch** — ML на CPU/GPU

_Примечание:_ Node.js + Express прокси больше не используется (удалён).

## 🔧 Доступные команды

```bash
# Фронтенд разработка
npm run dev          # Запуск dev сервера
npm run build        # Сборка для продакшена
npm run preview      # Предпросмотр собранного приложения
npm run lint         # Проверка кода

# Бэкенд разработка (в папке server/)
python pix2pix_server.py  # Запуск Python сервера
\


Если возникли вопросы:
- 📖 Читайте [SETUP.md](./SETUP.md)
- 📊 Изучите [REPORT.md](./REPORT.md)
- 🔍 Проверьте консоль браузера (F12) на ошибки

---

**Enjoy! 🎉**
